using UnityEngine;

        //  This "script" holds no logic, it only serves as an identifier for each setting element,
        //  so that we can automatically configure the rest.

public class GUISettingsElement : MonoBehaviour
{
    public GUISettingElementTypes ElementType;
}
